<% include partials/_header %>

<div class="container">
    <div class="row">
<!-- FORMULARIO -->
<div class="col-md-5 offset-md-3">
        <div class="card">
            <div class="card-body">
                <form action="/editar/<%=tarea._id %>" method="POST">
                    <div class="form-group">
                      <input type="text" name="titulo" placeholder="Insertar un titulo" class="form-control" value="<%=tarea.titulo  %>">
                    </div>
                    <div class="form-group">
                       <textarea name="descripcion"  cols="80"  placeholder="Insertar una descripcion" class="form-control" ><%=tarea.descripcion %></textarea>
                        </div>
                    <button type="submit" class="btn btn-warning btn-block">
                       Actualizar
                    </button>
                </form>
            </div>
        </div>
    </div>
    </div>

</div>


    <% include partials/_footer %>